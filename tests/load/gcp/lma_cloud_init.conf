#cloud-config

#users:
#- name: ubuntu

runcmd:
- touch /home/ubuntu/welcome.msg
- mkdir /run/mytempdir394857
#- systemctl disable systemd-tmpfiles-clean.timer --now
#- sudo -u ubuntu microk8s status --wait-ready
#- sudo -u ubuntu sh -c "until microk8s.kubectl rollout status deployments/hostpath-provisioner -n kube-system -w; do sleep 5; done"
#- sudo -u ubuntu sh -c "until microk8s.kubectl rollout status deployments/coredns -n kube-system -w; do sleep 5; done"
#- sudo -u ubuntu sh -c "until microk8s.kubectl rollout status daemonsets/nginx-ingress-microk8s-controller -n ingress -w; do sleep 5; done"
#- sudo -u ubuntu juju bootstrap --no-gui microk8s uk8s
#- sudo -u ubuntu juju add-model --config logging-config="<root>=WARNING; unit=DEBUG" --config update-status-hook-interval="60m" lma-load-test

